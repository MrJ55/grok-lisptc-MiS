# Permanence model

1. Process memory (ephemeral per Node eval).
2. Transcript image `mind/mind-image.ptc` (successful forms only).
3. GitHub push for cross-session restore.
4. Optional P3 vector vestiges.

Restore: `bash scripts/bootstrap.sh` then eval `(mis-ping)`.

`--save` only after success; `--checkpoint` → `*.prev.ptc`; `--scratch` is not permanent.
